{
  "servers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--browser=chrome"
        //"--headless",
        // "--allowed-origins=https://playwright.dev"
      ]
    }
  }
}
